apiVersion: v1
kind: Service
metadata:
  name: pocketid
  namespace: pocketid
  labels:
    app: pocketid
spec:
  selector:
    app: pocketid
  ports:
    - name: http
      port: 80
      targetPort: 1411
  type: ClusterIP
